const greet = greeting => name => console.log(`${greeting}  ${name}`);

greet('hi')('Umesh');
// how to return a function 
// over here greeting is the argument of a function and name is also an argument.   